spring:
  application:
    name: CoachingService
  datasource:
    url: jdbc:postgresql://localhost:5432/coaching_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8086

